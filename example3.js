const numberToBeChecked = -24;

const result = numberToBeChecked > 0 ? `The number ${numberToBeChecked} is Positive` : (numberToBeChecked < 0 ? `The number ${numberToBeChecked} is Negitive` :'The Number is 0')
console.log(result)